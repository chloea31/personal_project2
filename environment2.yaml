name: download_data_viruses
channels:
  - conda-forge
  - bioconda
  - anaconda
dependencies:
  - python>=3.10 
  - jupyter # => Command-line: conda install anaconda::jupyter
  - pandas 
  - sra-tools
  - pip